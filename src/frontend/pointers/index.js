define(["d3js", "variable"], function(){
  function drawSector(list, available) {}
  function demand(list, useStack){
    return 0;
  }
  
  return {
    drawSector: drawSector,
    demand: demand
  };
});