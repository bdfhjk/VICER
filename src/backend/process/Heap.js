define(function() {

    function Heap(memory) {
        this.memory = memory;
    }

    Heap.prototype.alloc = function alloc() {

    };

    Heap.prototype.free = function free(location) {
        
    };
    
});